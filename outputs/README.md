# Outputs

This directory is for local experiment artifacts.  Large generated files,
checkpoints, cached features, audio, and figures are ignored by git.

Markdown experiment summaries may be committed when they are useful for the
final report.
