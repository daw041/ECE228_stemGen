# Dataset

Local datasets are not tracked in git.  Configure the Slakh/BabySlakh path in
`configs/data_config.yaml`.

Expected audio-token branch input format is an extracted Slakh-style directory
with per-track stems that can be loaded by `src/dataset.py`.
